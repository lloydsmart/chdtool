name: CI

on:
  push:
  pull_request:

jobs:
  test-m3u:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install test deps
        run: |
          sudo apt-get update
          sudo apt-get install -y unzip zip make

      - name: Run tests
        run: make test
